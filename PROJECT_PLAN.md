# MonetaryEconomic 项目计划

## 1. 项目目标
- 输入：`data.xlsx`（央行沟通事件、时间、调性、文本）。
- 输出：
  - 每次沟通事件的 `Imp`（货币政策指数）。
  - 每次沟通事件的 `Ieo`（经济形势指数）。
  - 短语分布统计表（Excel）。
  - 实时计算词典（Excel）。

## 2. 已确认数据现状
- 数据主表：221 条记录，字段为 `序号/新闻来源/发布时间/调性/新闻内容`。
- 标签体系：
  - 货币政策相关调性：`宽松/稳健偏宽松/稳健/稳健偏紧缩/紧缩`。
- 已发现数据问题：
  - 2 条日期为 Excel 序列值（40466、40513），需修正为自然日期。
  - `新闻来源` 存在缺失（不影响指数计算，但需保留）。
  - 存在 1 组重复文本（需保留并在分析结果中标记）。

## 3. 方法路线（按文档要求落地）
1. 文本拆分与预处理  
   - 长文本拆为句/分句；保留事件与分句映射关系。  
   - 使用 `jieba` 分词。  
2. N-gram 短语抽取  
   - 生成连续短语，限制短语字符长度在 `[2, 37]`。  
   - 输出短语长度分布和频次统计。  
3. 短语标签与倾向判定  
   - 主题：`货币政策`、`经济形势`。  
   - 倾向：
     - 货币政策：`宽松/稳健/从紧`。
     - 经济形势：`正面/中性/负面`。
   - 概率规则：对每个短语统计在不同倾向样本中的出现占比；若某倾向概率 > 50%，收录进计算词典。  
4. 指数构建  
   - 对每次事件，按“短语概率 × 出现次数”计算倾向概率。  
   - `Imp = P(从紧) - P(宽松) + P(稳健)`。  
   - `Ieo = P(负面) - P(正面) + P(中性)`（在代码中设为可配置）。  
5. 导出与复现  
   - 输出清洗后事件表、短语分布表、计算词典、事件指数表。  
   - 提供一键运行脚本与 README。

## 4. 实施里程碑
- M1（今天）：搭建可运行流水线骨架，完成清洗、分句、分词、短语抽取、基础统计导出。
- M2：完成短语主题/倾向概率计算，产出词典与初版指数。
- M3：补充质量检查（缺失、异常、重复、时间排序），并给出结果解释说明。

## 5. 交付物
- `src/run_pipeline.py`：主执行脚本。
- `src/config.py`：参数与词典配置（阈值、关键词、长度范围等）。
- `requirements.txt`：依赖清单。
- `README.md`：环境创建与运行说明。
- `outputs/*.xlsx`：结果文件。

## 6. 验收标准
- 可以从原始 `data.xlsx` 一键跑通至 `outputs/`。
- 产生以下文件：
  - `events_clean.xlsx`
  - `phrase_distribution.xlsx`
  - `realtime_dictionary.xlsx`
  - `event_indices.xlsx`
- 指数文件包含事件级概率列与 `Imp/Ieo`。

## 7. 当前开工顺序（已执行）
1. 写入本计划文件。
2. 实现代码与配置文件。
3. 本地运行并验证输出结构。
4. 反馈你需要手动执行的 conda 环境指令。
